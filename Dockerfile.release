FROM opencfd/openfoam-default:2406

USER root

ENV DEBIAN_FRONTEND noninteractive

RUN wget -q -O - https://dl.openfoam.com/add-debian-repo.sh | sudo bash \
 && apt-get install -y python-is-python3 ack colordiff gdb git less plocate source-highlight vim-pathogen ripgrep rename gnuplot\
 && apt-get clean autoclean \
 && rm -rf /var/lib/apt/lists/*

RUN updatedb

